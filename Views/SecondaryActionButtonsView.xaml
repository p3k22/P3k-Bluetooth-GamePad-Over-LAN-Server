<UserControl x:Class="P3k_Bluetooth_GamePad_Over_LAN.Views.SecondaryActionButtonsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ContentControl Grid.Row="0" Grid.Column="0" x:Name="Select" Margin="5" />
        <ContentControl Grid.Row="0" Grid.Column="1" x:Name="Start" Margin="5" />
        <ContentControl Grid.Row="1" Grid.Column="0" x:Name="L1" Margin="5" />
        <ContentControl Grid.Row="1" Grid.Column="1" x:Name="R1" Margin="5" />
        <ContentControl Grid.Row="2" Grid.Column="0" x:Name="L3" Margin="5" />
        <ContentControl Grid.Row="2" Grid.Column="1" x:Name="R3" Margin="5" />
    </Grid>
</UserControl>